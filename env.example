# OpenAI API Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o
PROVIDER=openai

# Gemini (Google) optional
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-pro

# Summary & Assessor Profiles
SUMMARY_PROFILE=investor
ASSESSORS=professor,business_guru,investor,general
ASSESSOR_MODEL=gpt-4o

# --- HARVESTING CONFIGURATION ---
# To connect to a running Chrome browser for maximum stealth.
# 1. Close all Chrome instances.
# 2. Run from your terminal: "C:\Program Files\Google\Chrome\Application\chrome.exe" --remote-debugging-port=9222
# 3. Keep that terminal open. Now you can run harvest.js.
CHROME_REMOTE_DEBUG_URL=http://127.0.0.1:9222

# Rate limiting for harvesting (to avoid IP bans)
HARVEST_MAX_URLS_PER_HOUR=10

# Optional: Comma-separated list of proxy servers to rotate through
# Example: HARVEST_PROXY_LIST=http://user:pass@host1:port,http://user:pass@host2:port
HARVEST_PROXY_LIST=